namespace AlexaPowerTwitter.Models.AlexaModels
{
    class SessionAtributeNames
    {
        internal const string lastSlots = nameof(lastSlots);
        internal const string lastIntent = nameof(lastIntent);
    }
}
